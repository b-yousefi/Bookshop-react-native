class User {
  constructor(id, username, email, firstName, lastName, phoneNumber, password) {
    this.id = id;
    this.username = username;
    this.email = email;
    this.firstName = firstName;
    this.lastName = lastName;
    this.phoneNumber = phoneNumber;
    this.password = password;
  }
}

export default User;
